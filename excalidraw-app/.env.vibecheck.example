# VibeCheck demo build settings (set these as Vercel environment variables for the fork).
# The publishable key comes from `pnpm db:seed` output of the VibeCheck app.
VITE_APP_VIBECHECK_ORIGIN=https://vibecheck.patriciobcs.com
VITE_APP_VIBECHECK_KEY=REPLACE_WITH_pk_excalidraw_key
